// use gc (libgc)
#define _COTL_USE_GC

// use libgc from OS instead of ./gc
#define _COTL_USE_GLOBAL_GC

// use reference counter
#define _COTL_USE_REF_COUNT

// use unordered_map instead of map
#define _COTL_USE_UNORDERED_MAP
